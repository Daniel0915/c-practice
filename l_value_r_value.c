#include <stdio.h>

int main()
{
    /*
    연산자 : operator
    피연산자 : operand
    */

    /*
    Data object(object)
    L-value(object locator value) -> 값을 변경할 수 있음
    R-value(vlaue of on expression) -> 값을 변경할 수 없음
    */
   int i; // i object
   i = 1024; // i : L-value | 1024 : R-value
    return 0;
}